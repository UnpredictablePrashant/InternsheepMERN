version: '3.8'

services:
  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"
    networks:
      - app-network

  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"
    networks:
      - app-network

  kafka:
    image: confluentinc/cp-kafka:latest
    container_name: kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    depends_on:
      - zookeeper
    networks:
      - app-network

  authservice:
    build:
      context: ./backend/authService
      dockerfile: Dockerfile
    container_name: authservice
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://shrikarlonkar:wYkbmyFIj9iRDmPe@cluster0.gp5lvzy.mongodb.net/intsheep
      - JWT_SECRET=someshittyjwttokenisbeingused
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - KAFKA_BROKERS=kafka:9092
    depends_on:
      - redis
      - kafka
    networks:
      - app-network

  userservice:
    build:
      context: ./backend/userService
      dockerfile: Dockerfile
    container_name: userservice
    ports:
      - "5001:5001"
    environment:
      - PORT=5001
      - MONGO_URI=mongodb+srv://shrikarlonkar:wYkbmyFIj9iRDmPe@cluster0.gp5lvzy.mongodb.net/intsheep
      - JWT_SECRET=someshittyjwttokenisbeingused
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - AWS_REGION=ap-south-1
      - AWS_ACCESS_KEY_ID=XOXAKIA3M7AC6UDBHR37IQ2
      - AWS_SECRET_ACCESS_KEY=XOXnANkNLMMg/n9QAOp+25L2EbCHtSOMoL8KWz3/fwd
      - AWS_S3_BUCKET_NAME=internsheep
    depends_on:
      - redis
      - kafka
    networks:
      - app-network

  companyservice:
    build:
      context: ./backend/companyService
      dockerfile: Dockerfile
    container_name: companyservice
    ports:
      - "5002:5002"
    environment:
      - PORT=5002
      - MONGO_URI=mongodb+srv://shrikarlonkar:wYkbmyFIj9iRDmPe@cluster0.gp5lvzy.mongodb.net/intsheep
      - JWT_SECRET=someshittyjwttokenisbeingused
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - AWS_REGION=ap-south-1
      - AWS_ACCESS_KEY_ID=XOXAKIA3M7AC6UDBHR37IQ2
      - AWS_SECRET_ACCESS_KEY=XOXnANkNLMMg/n9QAOp+25L2EbCHtSOMoL8KWz3/fwd
      - AWS_S3_BUCKET_NAME=internsheep
    depends_on:
      - redis
      - kafka
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
